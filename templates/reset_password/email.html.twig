<h1>Bonjour,</h1>

<p>Pour réinitialiser votre mot de passe, merci de suivre le lien suivant:</p>

<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>

{#  mis en commentaire car pour le moment ca reste en anglais "hour" au lieu de "heure" Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.#}

<p>A bientot sur Sortir.com</p>
